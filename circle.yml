machine:
  timezone:
    Asia/Tokyo
  services:
    - docker
dependencies:
  cache_directories:
    - ~/cache
  pre:
    - ./build-docker.sh 6
test:
  override:
    - ./run-docker.sh 6
deployment:
  master:
    branch: master
    commands:
      - ./github-release.sh
